# ospanoff (c) // CMC MSU // 2014

This project is semestral task on C++;
Here is the realization of the Web-Server with cgi-bin, and script-lang;

Files & folders:
	lib/http_ans.cpp[.h] - realization of communication with web-browser
	lib/sock_creat.cpp[.h] - connecting client and server
	lib/Interpreter/ - Interpreter of script-lang
	server.cpp - main program; realization of multiconnection and connects all parts of server
	root/ - root dir of server to which client can accept

	.git* - git files & folders
	server - executable server file (Syntax: ./server port)

Server enables communication with browser: answers to popular requests of browsers, does cgi programs and supports script-lang